{% extends '::base.html.twig' %}

{% block content -%}
    <h1>Edition d'un utilisateur</h1>

    <hr />

    {% form_theme edit_form '::forms.html.twig' %}

    {{ form_start(edit_form, { 'attr': {'class': 'form-horizontal'} }) }}

    {{ form_end(edit_form) }}

        <a class="btn btn-success" href="{{ path('admin_user') }}">
            Retour à la liste
        </a>
    	

		<button class="btn btn-danger" onclick="myFunction()">
                Supprimer
            </button>
		<script  type="text/javascript">
        function myFunction() {
            var r = confirm("Êtes-vous sûr de vouloir supprimer l'utilisateur?");
            if (r == true) {

             var href="{{ path('admin_user_delete', {'id': entity.id}) }}";
             window.location = href;
            }
        }
        </script>

</ul>
{% endblock %}
